/* global describe, it */

import expect from '../expect'
import * as vientos from '../../src/vientos'

describe('imports', function () {
  it('is ok', function () {
    expect(vientos).to.be.ok()
  })
})
